'use client';

import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '../../../../components/ui/table';
import { useEffect,useState } from 'react';import { apiFetch } from '../../../../lib/api';import { Card,CardContent } from '../../../../components/ui/card';import { PageContainer } from '../../../../components/ui/page-container';import { PageHeader } from '../../../../components/ui/page-header';
interface Row{customerId:string;customerName:string;outstanding:string;overdue:string;advances:string;aging:Record<string,string>}
export default function ArSummaryPage(){const [rows,setRows]=useState<Row[]>([]);useEffect(()=>{apiFetch<Row[]>('/finance/ar/summary').then(setRows)},[]);return <PageContainer><PageHeader title="Customer-wise Accounts Receivable" description="Outstanding, advances and due-date aging from issued invoices"/><Card><CardContent className="overflow-x-auto p-0"><Table className="w-full text-sm"><TableHeader><TableRow className="border-b text-left"><TableHead className="p-3">Customer</TableHead><TableHead>Outstanding</TableHead><TableHead>Overdue</TableHead><TableHead>Advances</TableHead><TableHead>Current</TableHead><TableHead>1–30</TableHead><TableHead>31–60</TableHead><TableHead>61–90</TableHead><TableHead>91–180</TableHead><TableHead>&gt;180</TableHead></TableRow></TableHeader><TableBody>{rows.map(r=><TableRow className="border-b" key={r.customerId}><TableCell className="p-3 font-medium">{r.customerName}</TableCell><TableCell>{r.outstanding}</TableCell><TableCell>{r.overdue}</TableCell><TableCell>{r.advances}</TableCell><TableCell>{r.aging.current}</TableCell><TableCell>{r.aging.days1To30}</TableCell><TableCell>{r.aging.days31To60}</TableCell><TableCell>{r.aging.days61To90}</TableCell><TableCell>{r.aging.days91To180}</TableCell><TableCell>{r.aging.over180}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></PageContainer>}
