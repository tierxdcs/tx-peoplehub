'use client';
import {useEffect,useState} from 'react';import {apiFetch} from '../../../lib/api';import {Card,CardContent} from '../../../components/ui/card';import {PageContainer} from '../../../components/ui/page-container';import {PageHeader} from '../../../components/ui/page-header';
type Score={supplierId:string;supplierName:string;qualificationStatus:string;acceptanceRate:number;rejectionPpm:number;closedQmsAudits:number;majorFindings:number;minorFindings:number;qualityScore:number;rating:string};
export default function SupplierQuality(){const[rows,setRows]=useState<Score[]>([]);useEffect(()=>{apiFetch<Score[]>('/qms/supplier-scorecards').then(setRows)},[]);return <PageContainer><PageHeader title="Supplier Quality Scorecards" description="Live incoming acceptance, rejection PPM and audit-finding performance"/><Card><CardContent className="overflow-x-auto p-5"><table className="w-full text-sm"><thead><tr className="border-b text-left">{['Supplier','Qualification','Acceptance','Rejection PPM','QMS audits','Major / Minor','Score','Rating'].map(x=><th key={x} className="p-2">{x}</th>)}</tr></thead><tbody>{rows.map(x=><tr key={x.supplierId} className="border-b"><td className="p-2 font-medium">{x.supplierName}</td><td className="p-2">{x.qualificationStatus}</td><td className="p-2">{x.acceptanceRate}%</td><td className="p-2">{x.rejectionPpm.toLocaleString()}</td><td className="p-2">{x.closedQmsAudits}</td><td className="p-2">{x.majorFindings} / {x.minorFindings}</td><td className="p-2 font-semibold">{x.qualityScore}</td><td className="p-2">{x.rating}</td></tr>)}</tbody></table></CardContent></Card></PageContainer>}
