'use client';
import{FormEvent,useEffect,useState}from'react';import Link from'next/link';import{apiFetch}from'../../../lib/api';import{useQmsAccess}from'../../../lib/use-qms-access';import{Button}from'../../../components/ui/button';import{Card,CardContent}from'../../../components/ui/card';import{Input}from'../../../components/ui/input';import{Select}from'../../../components/ui/select';import{PageContainer}from'../../../components/ui/page-container';import{PageHeader}from'../../../components/ui/page-header';
type T={id:string;name:string;status:string;templateType:string};type I={id:string;inspectionNumber:string;inspectionType:string;status:string;batchOrSerial?:string};export default function Inspections(){const{isQmsHead}=useQmsAccess(),[templates,setTemplates]=useState<T[]>([]),[items,setItems]=useState<I[]>([]),[templateId,setTemplate]=useState(''),[batch,setBatch]=useState('');const load=()=>Promise.all([apiFetch<T[]>('/qms/templates'),apiFetch<I[]>('/qms/inspections')]).then(([t,i])=>{setTemplates(t.filter(x=>x.status==='APPROVED'));setItems(i)});useEffect(()=>{load()},[]);async function create(e:FormEvent){e.preventDefault();await apiFetch('/qms/inspections',{method:'POST',body:JSON.stringify({templateId,batchOrSerial:batch||undefined})});await load();}async function review(id:string,result:string){await apiFetch(`/qms/inspections/${id}/review/${result}`,{method:'POST',body:'{}'});await load();}return <PageContainer><PageHeader title="Quality Inspections" description="One execution engine for incoming, in-process, final, FAT and dispatch checks"/><Card className="mb-6"><CardContent className="p-5"><form className="flex gap-3" onSubmit={create}><Select required value={templateId} onChange={e=>setTemplate(e.target.value)}><option value="">Approved template</option>{templates.map(t=><option key={t.id} value={t.id}>{t.name} · {t.templateType}</option>)}</Select><Input value={batch} onChange={e=>setBatch(e.target.value)} placeholder="Batch / serial"/><Button type="submit">Start inspection</Button></form></CardContent></Card><Card><CardContent className="p-5">{items.map(i=><div className="flex items-center justify-between border-b py-3 text-sm" key={i.id}><Link className="font-medium text-primary" href={`/qms/inspections/${i.id}`}>{i.inspectionNumber} · {i.inspectionType} · {i.status}</Link>{isQmsHead&&i.status==='PENDING_REVIEW'&&<div className="flex gap-2"><Button size="sm" onClick={()=>review(i.id,'PASS')}>Pass</Button><Button size="sm" onClick={()=>review(i.id,'FAIL')}>Fail</Button></div>}</div>)}</CardContent></Card></PageContainer>}
