'use client';

import { TeamSection } from '../_sections/team-section';

/** Standalone route — the content lives in TeamSection (also used as a profile tab). */
export default function TeamPage() {
  return <TeamSection />;
}
