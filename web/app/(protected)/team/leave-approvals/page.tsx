'use client';

import { TeamLeaveApprovalsSection } from '../../_sections/team-leave-approvals-section';

/** Standalone route — content lives in the section (also a profile tab). */
export default function TeamLeaveApprovalsPage() {
  return <TeamLeaveApprovalsSection />;
}
