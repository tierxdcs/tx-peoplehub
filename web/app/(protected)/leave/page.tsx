'use client';

import { LeaveSection } from '../_sections/leave-section';

/** Standalone route — content lives in LeaveSection (also a profile tab). */
export default function MyLeavePage() {
  return <LeaveSection />;
}
