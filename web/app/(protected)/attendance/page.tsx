'use client';

import { AttendanceSection } from '../_sections/attendance-section';

/** Standalone route — content lives in AttendanceSection (also a profile tab). */
export default function MyAttendancePage() {
  return <AttendanceSection />;
}
