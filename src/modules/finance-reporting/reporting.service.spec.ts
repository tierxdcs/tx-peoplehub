import { Prisma } from '@prisma/client'; import { ReportingService } from './reporting.service';
describe('ReportingService controls',()=>{const access={assertCanViewFinance:jest.fn(),assertCanUseFinance:jest.fn(),assertAccountsHead:jest.fn()};const prisma={managementReportPack:{findUnique:jest.fn(),update:jest.fn()},yearEndRollover:{findUnique:jest.fn(),update:jest.fn()}};const s=new ReportingService(prisma as any,access as any);beforeEach(()=>jest.clearAllMocks());it('prevents self approval of a management pack',async()=>{prisma.managementReportPack.findUnique.mockResolvedValue({id:'p',status:'PENDING_APPROVAL',createdById:'head'});await expect(s.approvePack('p',{id:'head'} as any)).rejects.toThrow('cannot approve a pack they created')});it('computes inclusive reporting days',()=>expect((s as any).days({from:'2026-04-01',to:'2026-04-30'})).toBe(30));it('uses debit-positive balance convention',()=>expect(new Prisma.Decimal(100).minus(40).toString()).toBe('60'))});
