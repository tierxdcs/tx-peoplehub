import {ForbiddenException,Injectable}from'@nestjs/common';import{Role}from'@prisma/client';import{PrismaService}from'../../core/database/prisma.service';import{AuthenticatedUser}from'../../common/decorators/current-user.decorator';
@Injectable()export class DesignAccessService{constructor(private readonly prisma:PrismaService){}async accessFor(u:AuthenticatedUser){const e=await this.prisma.employee.findUnique({where:{id:u.id},select:{status:true,isDesignHead:true,vertical:{select:{code:true}}}});const designVertical=['DESIGN','ENGINEERING','RND'].includes(e?.vertical?.code?.toUpperCase()||'');return{isDesignUser:e?.status==='ACTIVE'&&(designVertical||e.isDesignHead||u.role===Role.SUPER_ADMIN),isDesignHead:e?.status==='ACTIVE'&&e.isDesignHead};}async assertUser(u:AuthenticatedUser){if(!(await this.accessFor(u)).isDesignUser)throw new ForbiddenException('Design access requires Design/Engineering membership');}async assertHead(u:AuthenticatedUser){if(!(await this.accessFor(u)).isDesignHead)throw new ForbiddenException('Only the designated Design Head may release design documents');}}
