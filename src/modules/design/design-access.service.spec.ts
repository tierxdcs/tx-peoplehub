import {ForbiddenException}from'@nestjs/common';import{Role}from'@prisma/client';import{DesignAccessService}from'./design-access.service';describe('DesignAccessService',()=>{const prisma:any={employee:{findUnique:jest.fn()}},service=new DesignAccessService(prisma),user:any={id:'e1',role:Role.EMPLOYEE};beforeEach(()=>jest.clearAllMocks());it('allows active Design and R&D vertical users to prepare designs',async()=>{prisma.employee.findUnique.mockResolvedValue({status:'ACTIVE',isDesignHead:false,vertical:{code:'DESIGN'}});await expect(service.assertUser(user)).resolves.toBeUndefined();await expect(service.assertHead(user)).rejects.toBeInstanceOf(ForbiddenException)});it('allows only the explicit Design Head to release',async()=>{prisma.employee.findUnique.mockResolvedValue({status:'ACTIVE',isDesignHead:true,vertical:null});await expect(service.assertHead(user)).resolves.toBeUndefined()});it('does not give Super Admin implicit release authority',async()=>{prisma.employee.findUnique.mockResolvedValue({status:'ACTIVE',isDesignHead:false,vertical:null});const admin={...user,role:Role.SUPER_ADMIN};await expect(service.assertUser(admin)).resolves.toBeUndefined();await expect(service.assertHead(admin)).rejects.toBeInstanceOf(ForbiddenException)});it('rejects inactive capability holders',async()=>{prisma.employee.findUnique.mockResolvedValue({status:'INACTIVE',isDesignHead:true,vertical:{code:'DESIGN'}});await expect(service.assertUser(user)).rejects.toBeInstanceOf(ForbiddenException)})});
